import Foundation
import CoreMotion
import Flutter


class PressureStreamHandler: NSObject, FlutterStreamHandler {
    let altimeter = CMAltimeter()
    private let queu = OperationQueue()
    
    func onListen(withArguments arguments: Any?, eventSink events: @escapin)
    
}
